import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Assuming you have an App component in a file named App.js

ReactDOM.render(
  <div>
    <App />
  </div>,
  document.getElementById("root")
);
